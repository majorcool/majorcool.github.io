const fs = require('fs');
const path = require('path');
const root = path.resolve(__dirname, '..');
const ignored = new Set(['.git', '.github', 'scripts', 'node_modules']);
const pick = (html, re) => (html.match(re)?.[1] || '').replace(/\s+/g, ' ').trim();
const esc = text => text.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
const projects = fs.readdirSync(root, { withFileTypes: true }).filter(e => e.isDirectory() && !ignored.has(e.name) && fs.existsSync(path.join(root, e.name, 'index.html'))).map(e => { const html = fs.readFileSync(path.join(root, e.name, 'index.html'), 'utf8'); return { href: '/' + e.name + '/', title: pick(html, /<title[^>]*>([\s\S]*?)<\/title>/i) || e.name, description: pick(html, /<meta[^>]+name=["']description["'][^>]+content=["']([^"']+)["']/i) || pick(html, /<meta[^>]+content=["']([^"']+)["'][^>]+name=["']description["']/i) || '托管目录：/' + e.name + '/' }; }).sort((a, b) => a.title.localeCompare(b.title, 'zh-CN'));
const cards = projects.map(p => '<li><a href="' + esc(p.href) + '">' + esc(p.title) + '</a><p>' + esc(p.description) + '</p><code>' + esc(p.href) + '</code></li>').join('');
const html = '<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1.0"><title>majorcool GitHub Pages</title><meta name="description" content="majorcool 在 GitHub Pages 上托管的项目索引。"><style>body{margin:0;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif;background:#f4f1ea;color:#161817;line-height:1.6}header{padding:72px 6vw;background:#171918;color:white}h1{margin:0 0 16px;font-size:clamp(42px,7vw,88px);line-height:1}header p{max-width:720px;color:rgba(255,255,255,.72);font-size:20px}main{padding:56px 6vw}ul{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:16px;padding:0;list-style:none}li{display:flex;flex-direction:column;min-height:210px;padding:24px;border:1px solid #ded7ca;border-radius:16px;background:#fffdf8;box-shadow:0 24px 70px rgba(22,24,23,.12)}li a{font-size:24px;font-weight:850;color:#171918}li p{color:#616b66}code{margin-top:auto}footer{padding:28px 6vw;background:#171918;color:rgba(255,255,255,.68)}</style></head><body><header><h1>majorcool GitHub Pages</h1><p>自动索引当前站点中带有 index.html 的托管项目。</p></header><main><ul>' + cards + '</ul></main><footer>自动生成：' + new Date().toISOString().slice(0,10) + '，共 ' + projects.length + ' 个项目。</footer></body></html>';
fs.writeFileSync(path.join(root, 'index.html'), html + '\n');
